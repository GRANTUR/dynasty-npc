name: Build & Deploy

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]

jobs:
  plugin:
    uses: GRANTUR/fortress-minecraft/.github/workflows/plugin-build.yml@main
    with:
      plugin-name: DynastyNPC
      artifact-id: dynasty-npc
      version: '1.0.0'
    secrets:
      FORTRESS_DEPLOY_TOKEN: ${{ secrets.FORTRESS_DEPLOY_TOKEN }}
